#!/usr/bin/env clitest --v0

using tempdir;

defer {
    $ ls -1 $PWD
    ! file

    cd $TEMP_DIR;

    $ rm -rf $PWD/subdir

    $ pwd
    *
}

$ pwd
%SET TEMP_DIR
! %{PATH}

$ mkdir subdir

# Avoid using `echo` because msys on Windows will munge the path
set PWD $PWD/subdir;

$ touch file
