#!/usr/bin/env clitest --v0

$ mktemp
%SET TEMP_FILE
*

background {
    $ for i in {1..10}; do printf "background\n" >> $TEMP_FILE; sleep .01; done && sleep 10
    %EXIT any
}

$ sleep .2 && cat $TEMP_FILE
repeat {
    ! background
}
